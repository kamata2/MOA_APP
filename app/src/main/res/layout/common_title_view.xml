<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rlCommonTitleViewCotainer"
    android:layout_width="match_parent"
    android:layout_height="@dimen/top_toolbar_height"
    android:background="@color/white"
    >

    <View
        android:id="@+id/viewCommonTitleDivider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_alignParentBottom="true"
        android:background="@color/gainsboro" />

    <RelativeLayout
        android:id="@+id/rlCommonTitleBack"
        android:layout_width="45dp"
        android:layout_height="match_parent"
        android:layout_above="@id/viewCommonTitleDivider"
        android:gravity="center_vertical"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/lbBackIc"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginStart="13.3dp"
            android:background="@drawable/top_toolbar_black_back_ic"
            android:contentDescription="@string/underline_description"
            android:paddingStart="@dimen/top_toolbar_back_ic_width"
            android:paddingEnd="0dp" />

    </RelativeLayout>

    <TextView
        android:id="@+id/btnCommonTitleText"
        style="@style/MoaFont"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/viewCommonTitleDivider"
        android:layout_marginLeft="45dp"
        android:layout_marginRight="45dp"
        android:ellipsize="end"
        android:gravity="center"
        android:singleLine="true"
        android:textColor="@color/black"
        android:textSize="17sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="타이틀입니다.." />

</RelativeLayout>