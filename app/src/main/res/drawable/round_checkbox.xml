<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_checked="false">
        <layer-list>
            <item android:drawable="@drawable/roundcheck_n"
                android:width="18dp"
                android:height="18dp"
                android:right="6.3dp">
            </item>
        </layer-list>
    </item>
    <!-- Checked -->
    <item android:state_checked="true">
        <layer-list>
            <!-- 이미지 영역 -->
            <item
                android:drawable="@drawable/roundcheck_p"
                android:width="18dp"
                android:height="18dp"
                android:right="6.3dp"/>
        </layer-list>
    </item>

</selector>